# Secrets — never commit these
*.pem
*.key
.env
.env.*
credentials.json
token.json
id_rsa*
id_ed25519*
.netrc
.npmrc

# age private key — NEVER commit
key.txt

# OS / editor junk
.DS_Store
._*
*.swp
*.swo
.idea/
node_modules/
